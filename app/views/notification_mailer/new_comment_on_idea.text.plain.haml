- @brief = @comment.commentable.brief
- @idea = @comment.commentable

:plain
  Account: #{@brief.account.name}
  Brief: #{truncate(@brief.title, :length => 40)}
  Link: #{brief_proposal_url(:id => @idea.id, :brief_id => @brief.id, :host => @brief.account.full_domain)}
  
  A comments has been left on the brief #{@comment.commentable.title} by #{@comment.user.full_name}.

  #{@comment.user.full_name} wrote:
  "#{@comment.comment}"
