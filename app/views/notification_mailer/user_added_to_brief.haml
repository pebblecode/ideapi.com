- @brief = @user_brief.brief 
:plain 
  Account : #{@brief.account.name}
  Brief   : #{truncate(@brief.title, :length => 40)}
  Link    : #{brief_url(@brief, :host => @brief.account.full_domain)}
  
  You’ve been invited to collaborate on the brief “#{@brief.title}” 
- if @user_brief.added_by_user.present? 
  :plain 
    by #{@user_brief.added_by_user.full_name}

=render :partial => 'footer'
