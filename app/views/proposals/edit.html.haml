=sub_nav do
  =breadcrumb_list

.col_1
  %h2 Editing your response
  
  = error_messages_for :proposal

  - form_for(current_object, :url => brief_proposal_path(parent_object, current_object), :html => { :method => :put, :multipart => true }) do |f|
    .boom_box
      = render :partial => "form", :locals => {:f => f}
      
    %p.submit
      = submit_tag "Save draft"
      
    %p.submit
      = submit_tag "Preview"
      .clear
    
    .hr
    
    %p.submit
      = submit_tag "Submit proposal"
      .clear
    
.col_2
  %p.emphasise=link_to 'back to brief', brief_path(parent_object), {:class => "awesome blue large", :style => "text-transform: uppercase" }