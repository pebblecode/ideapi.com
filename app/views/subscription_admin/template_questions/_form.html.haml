
= form.error_messages

%fieldset
  %h3 
    = form.label :body, "Question"
    = form.text_area :body, :rows => 3, :cols => 39
  
  %p
    = form.label :help_message
    = form.text_area :help_message, :rows => 5, :cols => 50
  
  %p
    = form.label :is_heading, 'Is this section a heading?'
    = form.check_box :is_heading
  
%p= submit_tag
