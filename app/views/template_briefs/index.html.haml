
=sub_nav do
  =breadcrumb_list
  -if current_user_can_create_briefs?
    %li{:class => "secondary"}
      =link_to 'Create template', new_template_brief_path
#dashboard.col_1

  -if flash[:note].present?
    .note
      %h3 Welcome to the dashboard. 
      
      %p=flash[':note']
      
  .brief_list
    %h2 
      Your templates
    -if @template_briefs.empty?
      .brief_item
        %h3 
          -if current_user_can_create_briefs?
            You have no templates -
            =link_to 'create a template', new_template_brief_path
            to get started.
          -else
            You have no briefs. Contact your account owner if you need the ability to create briefs
    -else  
      =render :partial => 'template_brief', :collection => @template_briefs
      
.col_2.dashboard
