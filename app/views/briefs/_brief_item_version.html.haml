%li.revision
  %span.delete
    = link_to "delete", timeline_event_url(object), :class => "remote-delete"
  %p
    
    %strong 
      Author changed section from:
  .item-revision-body
    :textile
      #{subject.body}
  %p
    %em="(#{comment_date(subject.brief_item.created_at)})"
    