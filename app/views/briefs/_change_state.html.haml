-form_for(brief) do |f|
  -if brief.published?
    =f.hidden_field '_call_state', :value => 'complete'
    .change-state
      =f.submit 'archive', :class => "action_button"
      %p.info
        Mark you document as completed to prevent it being changed by other users.  
    
  -elsif brief.complete?
    =f.hidden_field '_call_state', :value => 'reactivate'
    =f.submit 'reactivate', :class => "action_button"