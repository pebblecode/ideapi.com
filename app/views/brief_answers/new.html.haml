%h1 Creating brief_answer

= error_messages_for :brief_answer

- form_for(:brief_answer, :url => objects_url) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p= submit_tag "Create"

= link_to 'Back', objects_path
