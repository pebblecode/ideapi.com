%h1 Creating user

= error_messages_for :user

- form_for(:user, :url => objects_url, :html => { :multipart => true }) do |f|
  = render :partial => "form", :locals => {:f => f}
  %p
    = submit_tag "Create"
    .clear